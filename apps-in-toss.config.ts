import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'geuneul-jari',
  brand: {
    primaryColor: '#FFB223',
  },
  permissions: [],
  webBundleDir: 'public',
});
