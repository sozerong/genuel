# 버스명당

버스 노선과 승차 시각을 넣으면, 구간별로 **왼쪽/오른쪽 중 어느 창가에 앉아야 햇빛을 덜 받는지** 알려주는 웹앱.

한 줄 요약: 태양 방위각과 버스 진행 방위각의 차이만 구하면 답이 나온다. 나머지는 전부 계산이다.

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| 태양 위치 계산 (NOAA) | 완료 · 검증됨 |
| 구간 방위각 · 거리 계산 | 완료 |
| 좌우 노출량 판정 로직 | 완료 |
| UI (검색 → 추천 → 구간별 타임라인) | 완료 (단일 HTML 프로토타입) |
| 노선 데이터 | **하드코딩된 근사 좌표 10개 노선** ← 교체 대상 |
| TAGO API 연동 | 미완 (프록시 골격만 있음) |

`reference/prototype.html` — 동작하는 단일 파일 프로토타입. 계산 로직 전부 여기 있음.
`reference/proxy.mjs` — TAGO API 프록시 골격. 아직 실호출 검증 안 됨.

**이번 작업의 본질은 `prototype.html`의 `ROUTES` 배열을 실제 API 응답으로 갈아끼우는 것.** 계산 로직은 건드리지 말 것.

---

## 핵심 알고리즘

이 섹션의 수식과 상수는 검증된 것이다. 재유도하거나 "개선"하지 말 것. 바꿔야 한다면 먼저 물어볼 것.

### 1. 태양 위치 — NOAA 저정밀 알고리즘

입력: UTC 밀리초, 위도, 경도 → 출력: 고도(alt, °), 방위각(az, ° 북=0 시계방향)
오차 ±0.02°. 외부 라이브러리 불필요. 구현은 `prototype.html`의 `sunPos()` 참조.

한국 시각 처리는 반드시 `Date.UTC(y, mo-1, d, h-9, mi)`로 명시 변환할 것.
브라우저 로컬 타임존에 의존하면 해외 접속 시 깨진다.

### 2. 구간 방위각

정류소 두 점의 대권 방위각 (great-circle bearing):

```
θ = atan2( sinΔλ · cosφ₂ , cosφ₁·sinφ₂ − sinφ₁·cosφ₂·cosΔλ )
```

거리는 haversine.

### 3. 판정

```
Δ     = 태양방위각 − 진행방위각
sinΔ > 0  →  오른쪽 창에 햇빛  →  왼쪽에 앉기
sinΔ < 0  →  왼쪽 창에 햇빛    →  오른쪽에 앉기

측면 유입 강도  I = |sinΔ| · cos(고도)      (고도 ≤ 0 이면 I = 0)
구간 노출량     E_left  = 소요분 · max(0, −sinΔ) · cos(고도)
                E_right = 소요분 · max(0,  sinΔ) · cos(고도)
```

`cos(고도)`가 핵심이다. 정오에는 고도 74° → cos ≈ 0.28 → 지붕이 막아주므로 좌우 차이가 거의 없다.
오후 4~5시에는 고도 20~30° → 측면 유입 최대. 이 항을 빼면 앱이 무의미해진다.

**앞유리 정면광**: `cosΔ > 0.6` 이면 앞쪽 1~2열은 좌우와 무관하게 노출된다. 좌석도에만 반영, 좌우 판정에는 반영하지 않는다.

### 4. 침묵 규칙

- 구간 강도 `I < 0.13` → 그 구간은 "차이 없음"
- 전체 좌우 노출 차이 `|E_L − E_R| / max < 0.15` → 추천하지 않고 "차이 없음" 표시

**틀린 추천보다 침묵이 낫다.** 이 임계값을 낮춰서 항상 답이 나오게 만들지 말 것.

### 5. 구간 소요시간

현재: `거리 ÷ 21km/h + 정차 0.5분`

정확하지 않지만 **판정을 뒤집지 못한다.** 태양 방위각은 시간당 15° 이동하므로 5분 오차 = 1.25°.
그 정도로 뒤집히는 구간은 애초에 침묵 규칙에 걸린다. 개선은 선택사항이며 우선순위 낮음.

---

## 데이터 소스

### TAGO 버스노선정보 (공공데이터포털)

```
기관코드   1613000
서비스     BusRouteInfoInqireService
베이스     https://apis.data.go.kr/1613000/BusRouteInfoInqireService
문서       https://www.data.go.kr/data/15098529/openapi.do   (데이터셋명: 국토교통부_(TAGO)_버스노선정보)
```

**주의**: `data.go.kr` 검색에서 나오는 비슷한 이름의 `국토교통부_버스노선`(데이터셋 `15142030`)은 **다른 API다.**
최근 개편되어 base URL이 `apis.data.go.kr/1613000/BusRoute`로 바뀌었고 오퍼레이션도 `getBusRoute` 하나뿐이며
경유 정류소 목록·좌표를 아예 제공하지 않는다. 착각해서 그쪽 문서를 보고 작업하지 말 것 — 위 15098529가 맞는 문서다.

| 오퍼레이션 | 용도 |
|---|---|
| `getCtyCodeList` | 도시코드 목록. 키 활성화 확인용으로도 쓴다 |
| `getRouteNoList` | 도시별 노선 목록 (`routeid`, `routeno`, `startnodenm`, `endnodenm`) |
| `getRouteAcctoThrghSttnList` | **경유 정류소 목록.** 이게 핵심 |

`getRouteAcctoThrghSttnList` 응답 필드 (실측 확인, 문서와 다름):

```
nodeid      정류소ID
nodenm      정류소명
nodeno      정류소 번호
nodeord     정차 순서
gpslati     위도
gpslong     경도
routeid     노선ID
```

**`updowncd`(상/하행) 필드는 없다.** 문서에는 있다고 나올 수 있으나 실제 응답엔 안 온다 (천안시 201번 노선 실측).
방향은 정류소 필드가 아니라 **routeId 자체**로 갈린다 — 반대 방향이 있으면 `getRouteNoList` 결과에
별도 `routeid`로 따로 나온다. 상행/하행 선택 UI를 만들지 말 것, 노선 검색 결과에서 방향별로 다른 routeId를 고르게 할 것.

### TAGO 버스위치정보 (실시간)

```
서비스   BusLcInfoInqireService
베이스   https://apis.data.go.kr/1613000/BusLcInfoInqireService
오퍼레이션  getRouteAcctoBusLcList (cityCode, routeId → 그 노선을 현재 운행 중인 차량 목록)
문서     https://www.data.go.kr/data/15098533/openapi.do
```

같은 기관(1613000)의 형제 서비스라 **TAGO_KEY 그대로 재사용**, 인증/에러 스키마도 동일. `nodeord`가 각 차량이
지금 지나는 정류소 순번이므로, 내가 탈 정류소의 `ord`와 비교해 "N정류장 전"을 계산한다. 서울은 노선정보와
마찬가지로 이 API 관할 밖 — `/api/buslocation`은 서울이면 빈 배열을 바로 반환한다.
위치는 계속 바뀌는 값이라 노선정보(24시간)와 달리 15초 TTL로 캐시한다.

### 이 API의 함정 (실제로 겪은 것들)

1. **CORS 차단.** `Access-Control-Allow-Origin` 헤더가 없다. 확인 완료.
   브라우저에서 직접 `fetch` 불가 → **프록시 서버 필수.**
2. **인증키는 서버에만.** 프론트엔드 JS에 넣지 말 것. 환경변수 `TAGO_KEY`로 주입.
3. **결과 1건이면 배열이 아니라 객체로 온다.** `items.item`을 항상 `Array.isArray()` 체크할 것.
4. **`nodeord` 정렬을 신뢰하지 말 것.** 응답 순서가 보장되지 않으므로 직접 정렬한다.
5. **인증 실패는 다른 스키마로 온다.** 정상은 `response.header.resultCode`, 실패는 `OpenAPI_ServiceResponse.cmmMsgHeader`. 둘 다 처리할 것.
6. **에러 응답이 XML일 수 있다.** `_type=json`을 줘도 그렇다. `JSON.parse` 실패를 잡을 것.

### 현재 블로커

발급받은 인증키가 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` (코드 30)를 반환한다.
인코딩키/디코딩키 양쪽, 3개 서비스 모두 동일. 키 상태 문제이지 코드 문제가 아니다.

작업 시작 전 이걸로 확인할 것:

```bash
curl "https://apis.data.go.kr/1613000/BusRouteInfoInqireService/getCtyCodeList?serviceKey=$TAGO_KEY_ENCODED&_type=json"
```

도시코드 목록이 나오면 진행. 여전히 코드 30이면 사용자에게 알리고 멈출 것 — 우회하지 말 것.
(천안 도시코드를 34010으로 가정해 뒀으나 **미확인.** 이 응답에서 실제 값을 확인할 것.)

---

## 아키텍처

```
브라우저 (정적 파일)
    │  fetch /api/*
    ▼
프록시 (Node, 키 보관 + CORS 해소)
    │  https
    ▼
TAGO API
```

프록시 엔드포인트:

```
GET /api/cities                                 도시코드 목록
GET /api/routes?cityCode=&routeNo=              노선 검색
GET /api/stops?cityCode=&routeId=               경유 정류소 + 좌표
GET /api/buslocation?cityCode=&routeId=         실시간 운행 차량 (nodeOrd, nodeName, vehicleNo)
```

정류소 응답은 프론트가 그대로 쓸 수 있는 모양으로 정규화해서 내보낸다:

```json
[{ "ord": 1, "name": "천안역", "lat": 36.8095, "lon": 127.1487 }]
```

캐싱: 노선 경로는 거의 안 바뀐다. 프록시에 인메모리 캐시(TTL 24시간)를 두면 API 호출량이 크게 준다. 일일 트래픽 제한이 있으므로 권장.

---

## 설계 원칙

**하지 말 것**

- 계산 로직 리팩터링. 검증된 코드다.
- 임계값(0.13 / 0.15)을 낮춰 항상 추천이 나오게 만들기.
- 지도 화면 추가. 노선 검색 앱은 이미 많다. 좌표/버스 위치는 텍스트(예: "N정류장 전")로만 보여주고
  지도 UI(마커, 라이브러리)는 넣지 않는다 — 사용자 확인 후 명시적으로 이 선을 유지하기로 함.
- 건물 그림자 반영. v1 범위 밖. 고도 20° 미만 구간에 "신뢰도 낮음" 표시로 대체한다.
- 좌표를 손으로 채워넣기. API가 안 주면 그 노선은 지원하지 않는다고 표시한다.

**할 것**

- 실패는 조용히 넘어가지 말고 사용자에게 무엇이 왜 안 됐는지 말한다.
- 추정값(소요시간)과 실측값(좌표)을 UI에서 구분해 보여준다.
- 반응형, 키보드 포커스, `prefers-reduced-motion` 존중.

## 디자인 토큰

프로토타입에서 확정된 값. 유지할 것.

```css
--ink:#101A24   --slate:#33485E  --paper:#E9EDF1  --card:#FFFFFF  --line:#C9D3DB
--sun:#FFB223   --hot:#EF6C1A    --shade:#2F6C86  --shade-lt:#5F9BB2
```

- 디스플레이: Black Han Sans (제목·노선번호에만, 절제해서)
- 본문: IBM Plex Sans KR
- 수치·시각: IBM Plex Mono (tabular-nums)

노랑·주황 = 햇빛, 청록 = 그늘. 의미가 있는 색이므로 장식으로 쓰지 말 것.

## 검증

1. **태양 계산**: 특정 날짜·좌표의 일출·일몰·남중시각을 기상청 값과 대조.
2. **판정**: 정동/정서로 달리는 구간을 정오와 오후 5시로 각각 계산 → 정오는 "차이 없음", 오후 5시는 뚜렷한 한쪽이 나와야 한다.
3. **현장**: 실제 노선 1개를 타면서 5분 간격으로 좌/우 체감 기록 → 예측과 대조. 곡선 많은 노선 1개, 직선 위주 1개.
